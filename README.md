## Face-Mask-Detection

Libraries will be used for the projects and there roles.

glob       → find images
os/Path   → create folders
shutil    → move files
yaml      → dataset config
sklearn   → split train/val
YOLO      → train model
